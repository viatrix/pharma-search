# pharma-search

The project is based on Serverless, Typescript, TypeORM, Amazon RDS (Postgres).


#### Compile project:

```
npm run compile
```

#### Start project locally:

```
npm run start
```

#### Run tests:

```
npm run test
```
